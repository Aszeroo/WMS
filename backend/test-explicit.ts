import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient({ datasources: { db: { url: process.env.DATABASE_URL } } }); prisma.$connect().then(() => console.log('Connected successfully')).catch(e => console.error('Connection failed:', e)).finally(() => prisma.$disconnect());
